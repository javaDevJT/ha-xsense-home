name: HACS

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  hacs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hacs/action@main
        with:
          category: integration
